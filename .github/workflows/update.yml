name: Update Fund Report

on:
  schedule:
    # Every weekday at 18:30 UTC (after European market close)
    - cron: "30 18 * * 1-5"
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate sector data (US + STOXX 600)
        env:
          EODHD_API_TOKEN: ${{ secrets.EODHD_API_TOKEN }}
        working-directory: sector_scripts
        run: |
          python eodhd_us_sector_report.py
          python eodhd_stoxx600_sector_report.py

      - name: Generate report
        run: python compare_funds.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
